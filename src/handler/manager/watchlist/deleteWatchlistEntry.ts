
import { Response } from "express";
import { AuthenticatedRequest } from "../../../lib/middleware/requireAuth";

const deleteWatchlistEntry = async (req: AuthenticatedRequest, res: Response) => {
    
}